#!/bin/bash

PWD=$(pwd)

ln -is $PWD/helix ~/.config/helix
ln -is $PWD/yazi ~/.config/yazi
ln -is $PWD/zellij ~/.config/zellij
ln -is $PWD/zshrc ~/.zshrc
ln -is $PWD/quinn.zsh-theme ~/.oh-my-zsh/custom/themes/quinn.zsh-theme