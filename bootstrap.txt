sudo pacman -S vivaldi wl-clipboard ghostty

# Install oh my zsh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# Install homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Install mise
curl https://mise.run | sh

brew install yazi lazygit neovim zellij

mise use -g rust@nightly

# Clone dotfiles
# Add your rsa key to github and gitlab first!
mkdir -p ~/code/utils
cd ~/code/utils
git clone git@github.com:quinnromanek/dotfiles.git
cd ~/code/utils/dotfiles && cargo install rotz && rotz -d $(pwd) link

# Bevy deps
sudo pacman -S libx11 pkgconf alsa-lib libxcursor libxrandr libxi lld clang
mkdir -p ~/code/games
git clone git@gitlab.com:quinnromanek/apocalypse.git
